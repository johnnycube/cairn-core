name: Build & push core image

# Builds the Cairn core image (docker/Dockerfile.core) with BuildKit and, on a
# successful build, pushes it to the GitHub Container Registry tagged by git tag
# + commit hash. The image is a single DISTROLESS binary (nonroot, no shell)
# that serves both the API and the embedded web SPA — `cairn serve` answers /
# and /api + /cairn.v1.*.
#
# Proto code (gen/) is generated OFFLINE: the test job runs inside the shared
# build-base toolchain image (local buf plugins on PATH, no BSR), and the
# Dockerfile's own `proto` stage (FROM build-base) regenerates it in-image — so
# this workflow needs no setup-go / buf-setup / proto cache.
#
# Registry: ghcr.io/johnnycube/cairn-core
# Auth: the built-in GITHUB_TOKEN (packages: write) — no extra secret needed.
# Depends on the PUBLIC ghcr.io/johnnycube/cairn-build-base:1 package (published
# by build-base.yml); a public package pulls without credentials.
#
# Supply chain: pushed images carry BuildKit SLSA provenance + SBOM
# attestations, are signed keyless with cosign (Fulcio cert bound to this
# workflow's OIDC identity, logged in Rekor), and get a GitHub provenance
# attestation (verifiable via `gh attestation verify`). Verify with:
#   cosign verify ghcr.io/johnnycube/cairn-core:<tag> \
#     --certificate-oidc-issuer https://token.actions.githubusercontent.com \
#     --certificate-identity-regexp \
#       '^https://github.com/johnnycube/cairn-core/\.github/workflows/build-core\.yml@.*'
#
# Triggers:
#   - push to the default branch  → publishes  sha-<short>  (+ latest)
#   - push of a v* git tag        → publishes  <tag>  + sha-<short>
#   - pull_request                → runs the test job only, as a merge gate;
#                                   the image job is skipped entirely so PRs
#                                   can never reach the registry

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:

concurrency:
  group: build-core-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/johnnycube/cairn-core
  BUILD_BASE: ghcr.io/johnnycube/cairn-build-base:1

jobs:
  # Gate: the core unit suite must pass before the image is built. Provider
  # workers live in their own repos with their own CI. These tests are DB-free
  # — the integration suite lives behind the `integration` build tag and isn't
  # run.
  #
  # Runs inside the build-base toolchain image: Go + buf + local proto plugins
  # are already on PATH, so `make proto` runs offline and there's no setup step.
  test:
    name: Core tests
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/johnnycube/cairn-build-base:1
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # The checkout is owned by a different uid than the container user; mark
      # it safe so git works and go's -buildvcs stamping doesn't fail the build.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      # Cache the Go module + build caches (setup-go isn't used inside the
      # container, so cache them explicitly).
      - name: Cache Go
        uses: actions/cache@v4
        with:
          path: |
            /go/pkg/mod
            /root/.cache/go-build
          key: go-core-${{ hashFiles('go.sum') }}
          restore-keys: go-core-
      # Proto code (gen/) is regenerated offline (the build-base image carries
      # the local buf plugins, so no buf.build calls).
      - name: Generate protos
        run: make proto
      - name: Core packages
        shell: bash
        run: |
          go build ./...
          go test ./...

  build-push:
    name: Build & push cairn (core)
    runs-on: ubuntu-latest
    # Only build/push once the core tests are green — and only on push events
    # (main / v* tags). PRs are gated by the test job alone and never build or
    # push an image.
    needs: test
    if: github.event_name == 'push'
    # id-token/attestations only here — the test job stays unprivileged.
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Enables BuildKit (buildx) for the build below.
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Derive image tags: the git tag (on tag pushes), the short commit SHA
      # always, and `latest` on the default branch.
      - name: Compute tags & labels
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=ref,event=tag
            type=sha,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push (BuildKit)
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.core
          # Publish only from main or a v* release tag; PRs/feature branches
          # build as a gate but never push to the registry.
          push: ${{ github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # BuildKit-level SLSA provenance + SBOM, attached to the image index.
          provenance: mode=max
          sbom: true
          # Surfaced into the binary's build info (-ldflags in the Dockerfile).
          build-args: |
            BUILD_BASE=${{ env.BUILD_BASE }}
            VERSION=${{ github.ref_type == 'tag' && github.ref_name || github.sha }}
            COMMIT=${{ github.sha }}

      # Keyless sigstore signing — one signature on the pushed digest covers
      # every tag of this push. Gated like `push:` so it never runs unpushed.
      - name: Install cosign
        if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
        uses: sigstore/cosign-installer@v4.1.2

      - name: Sign image (keyless)
        if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
        env:
          COSIGN_YES: "true"
        run: cosign sign "${{ env.IMAGE }}@${{ steps.build.outputs.digest }}"

      # GitHub-native provenance attestation (Attestations tab, `gh attestation
      # verify`), stored alongside the image in ghcr.
      - name: Attest build provenance
        if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.IMAGE }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
